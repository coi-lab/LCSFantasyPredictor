# Safety

Do not use destructive Git operations, expose secrets, mutate raw data, weaken
tests, overwrite unrelated user work, or broaden approved scope. Do not run
`git reset`, `git clean`, `git checkout --`, `git restore`, `git stash`,
rebase, force operations, merge, commit, or push unless a later human-approved
task explicitly authorizes the specific action.
