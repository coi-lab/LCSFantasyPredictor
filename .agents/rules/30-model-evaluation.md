# Model evaluation

For model work load
`../skills/verify-model-change/SKILL.md`. Reproduce the frozen baseline,
compare identical cutoff-safe chronological rows, preserve feature gates, and
report 2026 as exposed rather than blind evaluation. Unit tests establish
calculation behavior, not predictive improvement.
