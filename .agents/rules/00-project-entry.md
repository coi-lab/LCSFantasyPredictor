# Entry

Read `AGENTS.md`, the human-approved task plan, the relevant shared documents
under `docs/agent/`, and only the matching `.agents/skills/` definition before
editing. Record task ID, current Git state, approved scope, baseline commands,
and pre-existing failures. Use `.venv/bin/python` for project commands.
