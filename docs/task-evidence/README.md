# Legacy Phase 1 task evidence

Completed packets under this directory are retained as historical Phase 1
evidence. New task runs use `.agent-runs/<task-id>/` as the canonical evidence
location.

`task_manifest_schema.json` remains a shared legacy-format reference for
structured manifests; it does not change the canonical storage location.
