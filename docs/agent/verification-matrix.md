# Verification matrix

Path moves require targeted consumer checks plus the full unit suite,
compilation, `git diff --check`, and Git status. Dashboard moves additionally
require exporter, server, browser-fetch, and historical-snapshot checks.
