# Shared project knowledge

Production modules are `champion_prediction/`, `fantasy_prediction/`,
`data_pipeline/`, `learning/`, and `rag/`. `learning/learnings.json` is
experimental application state, not agent memory.

Oracle's Elixir files are raw professional-match data. Official market
snapshots are immutable. Dashboard current JSON is generated data; weekly
lineup snapshots are historical evidence. Browser producer and consumer paths
must change together.
